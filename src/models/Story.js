class Story {
    constructor(book, beginChapter, storyName, url='') {
        this.book = book
        this.beginChapter = beginChapter
        this.storyName = storyName
        this.url = url
    }
}

export default Story